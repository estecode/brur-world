# Explicit Sweden OSM cache/build logging

Issue: #204

The Sweden build prints the authoritative source path and SHA-256, clear phase separators, and the exact route-cache input consumed by downstream roads, routing, traffic, background, POI, building, and search builders.

This is observability only; the source-cache and runtime-data ownership contracts remain unchanged.
