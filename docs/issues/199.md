# Ensure SafeLink/real-data prepare paths use OSM route caches

Follow-up to #197/#198. All project-owned SafeLink/playtest paths that need real Sweden OSM data must prepare source data through `osm_source_cache.py` rather than calling downstream builders directly on the authoritative Sweden PBF. Deterministic fixture-only tests may continue to bypass the cache boundary.

Acceptance:
- GPS playtest routing rebuild uses the `highways` route cache.
- `pr_check.sh` routing rebuild uses the `highways` route cache.
- `pr_check_local.sh` routing rebuild uses the `highways` route cache.
- Full game playtest continues to use `build_sweden.py` (already cache-backed).
- Tests guard these production/SafeLink paths from regressing to direct PBF routing builds.
- Hosted/headless validation green.
