name: GeoDot POC validation

on:
  pull_request:
    branches:
      - "main"
      - "issue/342-geodot-poc"

permissions:
  contents: read

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "GEODOT_WORKFLOW_SYNTAX=PASS"
